{
   "icon": "",
   "context": {
       "person": true,
  		"place": false,
  		"expression": "ageInYears(contact)>=12 && ageInYears(contact)<=52 && contact.type === 'person' && contact.sex === 'female' && user.parent.type === 'health_center'"
   	}
}
