{
   "icon": "treatment",
   "context": {
       "person": true,
  		"place": false,
  		"expression": "user.parent.type !== 'district_hospital' && ((!contact && user.parent.type != 'district_hospital') || (contact.type !== 'person'))"
   	}
}
